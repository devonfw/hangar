version: '3'

tasks:
  build:
      cmds:
      # Specify the build steps using bash commands here
      - npm install


  build-configuration:
      # Specify build target directory here
      vars: { 
        TARGET_DIRECTORY: "./"
      }

      cmds:
      # DO NOT CHANGE THIS!
      - echo "##vso[task.setvariable variable=targetPath;]{{.TARGET_DIRECTORY}}"

